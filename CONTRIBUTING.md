# Contributing

- Keep API keys and secrets out of tracked files.
- Use environment variables or the documented local configuration approach for credentials.
- Test search, geolocation, loading, error, and empty states before submitting changes.
- Keep UI, API, and accessibility changes focused and documented.

Use concise commit messages such as `feat: add forecast details`, `fix: handle geocoding errors`, or `docs: update setup`.
